#import
import serial

#make the port
port = serial.Serial("COM3", baudrate=115200)

#send the trigger code
port.write(TRIGGER) # TRIGGER can be binary (b'0-255') or a string
port.flush()


#at the end:
port.close()
